@import "std"

async fn main() {
    // Offline-friendly: closed high port → err arm
    match await std.http.get("http://127.0.0.1:59999/") {
        ok(res) => { std.log("unexpected ok status=$1", res.status()) }
        err(e) => { std.log("http err ok") }
    }
}
